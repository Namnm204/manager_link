import { AllowedPath, Config, Request } from './types';
export declare const getConfigOptions: (config: Partial<Config>) => Config;
export declare const getSecFetchHeaders: (req: Request) => {
    secFetchSite: string;
    secFetchMode: string;
    secFetchDest: string;
};
export declare const matchAllowedURL: (list: (string | AllowedPath)[], req: Request) => boolean;
